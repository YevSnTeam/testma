<p>
	<b><%= @category.name %> </b>
</p>

<table>
<% @category.todos.each do |todo| %>
	<tr>
		<td><%= todo.name %></td>
	    <td><%= link_to 'Anzeigen', todo %></td>
	    <td>Anzahl gewählt</td>
	    <td>Anzahl erledigt</td>
	</tr>
<% end %>
</table>